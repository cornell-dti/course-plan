import { Course } from '../../types';
declare const _default: {
    ilrCoreRequirements: ((course: Course) => boolean)[];
    ilrFWS: (course: Course) => boolean;
    ilrAdvanceWriting: (course: Course) => boolean;
};
export default _default;
