import { Course } from '../../types';
declare const _default: {
    englishTotalCredits: (course: Course) => boolean;
    englishPre1800: (course: Course) => boolean;
    english4000OrAbove: (course: Course) => boolean;
};
export default _default;
