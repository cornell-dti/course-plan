import { Course } from '../../types';
declare const _default: {
    csIntroProgramming: ((course: Course) => boolean)[];
    csCore: ((course: Course) => boolean)[];
    csPracticumOrProject: (course: Course) => boolean;
};
export default _default;
